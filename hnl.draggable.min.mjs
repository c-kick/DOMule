import eventHandler from"./hnl.eventhandler.mjs";export const NAME="draggable";function restoreSnappingGracefully(scrollElement){const scrollingXAxis=true;const snapItem=document.querySelectorAll(`.${scrollElement.dataset.snapItems}`)[0];const gap=parseInt(window.getComputedStyle(snapItem.parentElement).getPropertyValue(scrollingXAxis?"column-gap":"row-gap"),10);const additionalGap=isNaN(gap)?0:gap;const scrollPosition=scrollingXAxis?scrollElement.scrollLeft:scrollElement.scrollTop;const scrollSize=
scrollingXAxis?scrollElement.scrollWidth:scrollElement.scrollHeight;const scrollerSize=scrollingXAxis?scrollElement.offsetWidth:scrollElement.offsetWidth;const slideItemSize=(scrollingXAxis?snapItem.offsetWidth:snapItem.offsetHeight)+additionalGap;const closestSnap=Math.round(scrollPosition/slideItemSize);const tolerance=2;let timeout=null;function waitToRestoreSnapping(){clearTimeout(timeout);let wait=scrollPosition%slideItemSize?150:0;timeout=setTimeout(()=>{scrollElement.dataset.scrollSnapping=
"true";scrollElement.removeEventListener("scroll",waitToRestoreSnapping);scrollElement.__busy=false},wait)}if(Math.abs(scrollPosition)<tolerance||Math.abs(scrollPosition+scrollerSize-scrollSize)<tolerance){console.log("Already at snapping point, no need to restore snapping gracefully");scrollElement.dataset.scrollSnapping="true";scrollElement.removeEventListener("scroll",waitToRestoreSnapping)}else{scrollElement.scrollTo({top:scrollingXAxis?0:closestSnap*slideItemSize,left:scrollingXAxis?closestSnap*
slideItemSize:0,behavior:"smooth"});scrollElement.__busy=true;scrollElement.removeEventListener("scroll",waitToRestoreSnapping);scrollElement.addEventListener("scroll",waitToRestoreSnapping)}}const makeDraggable=(container,changeCursor=false)=>{let isDragging=false,startX=0,scrollLeft=0;container.dataset.scrollSnapping="true";const setCursor=cursor=>container.style.cursor=cursor;const mouseMove=e=>{if(!isDragging)return;e.preventDefault();const x=e.pageX-container.offsetLeft;container.scrollLeft=
scrollLeft-(x-startX)};const stopEverything=()=>{isDragging=false;restoreSnappingGracefully(container);if(changeCursor)setCursor("grab");container.removeEventListener("mousemove",mouseMove);document.removeEventListener("mouseup",stopEverything)};container.addEventListener("mousedown",e=>{container.dataset.scrollSnapping="false";isDragging=true;startX=e.pageX-container.offsetLeft;scrollLeft=container.scrollLeft;if(changeCursor)setCursor("grabbing");container.addEventListener("mousemove",mouseMove);
document.addEventListener("mouseup",stopEverything)})};export function init(elements){eventHandler.addListener("docShift",()=>{elements.forEach(function(element){makeDraggable(element)})})};
