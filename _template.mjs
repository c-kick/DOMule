/**
 * Module template
 */

//define name for the module, usable in logging etc.
export const NAME = 'module template';

export function init(elements) {
  //do stuff with the elements that wanted this module through `data-requires`
}